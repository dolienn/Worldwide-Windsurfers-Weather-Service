spring:
  mvc:
    static-path-patterns: /**
  resources:
    static-locations: classpath:/static
  profiles:
    active: dev
  application:
    name: Worldwide Windsurfers Weather Service
server:
  port: ${PORT:8080}

